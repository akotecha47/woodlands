# WOODLANDS SYSTEM AUDIT #3 — 2026-08-18

**Read-only code audit, run in parallel with Aman's role-by-role browser pass.** One half of a merged fix queue: code findings are `C-nn`, browser findings will be `U-nn`. IDs are stable — never reused or renumbered.

**Method.** Read every file under `src/` (all 69), both Edge Functions, `scripts/apply-sql.ps1`, and targeted migrations (055, 061 in full; 059/060/062 via their proven records). Measured live production read-only through `scripts/apply-sql.ps1 -ReadOnly` (Management API, connects as `postgres` — used only for facts about schema/policies/rows, never for what a role can do). Every live claim below quotes a measured result. Nothing was modified except this file. Severity is judged **only** against the client walkthrough in ~10 days: P1 = will visibly break or mislead during the walkthrough · P2 = wrong, survivable in a demo · P3 = cleanup, post-walkthrough.

**Live baseline measured 18 Aug 2026:** 8 user_profiles (all active) · 1 event · 5 payments · 4 bill items · 2 allocations · 24 rooms · 311 fm_holders (6 `Z` placeholders) · 687 fm_visits · 62 staff · 192 requisitions · 2 posted bar_count_sessions (both 17 Aug) · 559 bar_count_lines · 727 stock_movements · 1135 current_stock rows · 15 attendance_records · 3 table_bookings · 2 v_stock_consumption rows.

---

## 1. DECISIONS YOU'RE ASKING

Every fork found. If a finding below references a decision, it is one of these.

- **D-1 (for C-07) — Movement Ledger requisition pairs: fix the display or the data?** 94 fulfilled requisitions have written ± ledger pairs typed `'requisition'`; `collapsePairs` only collapses `issue`/`transfer`, so every fulfil renders twice.
  (a) add `'requisition'` to `PAIRED_TYPES` (display-only, no data touched);
  (b) make fulfil pass/derive `'issue'` like `transfer_stock` does, and re-type the 188 existing rows in a data-op (one physical event, one ledger type — the 057 doctrine);
  (c) both. Note (b) changes what the Requisition type filter shows.
- **D-2 (for C-12) — `shift_settings` re-tag.** The 12 shift rows still say `Restaurant Bar` and `Front Desk`; data-ops/003 never covered this table, so Main Bar and Front Office staff match no shift. Re-tag by data-op (extending 003), or hand-edit in Attendance → Settings? Note the side effect: once `Restaurant Bar` becomes `Main Bar`, Main Bar staff start matching **rotating** Week A/B shifts, and the rotating-week selection is hard-coded to `'A'` (C-05) — decide whether the rotating rows should survive at all.
- **D-3 (for C-37) — Delete Event vs payments.** `event_payments.event_id` is `ON DELETE NO ACTION` (measured), so deleting an event that has payments fails with a raw FK error while the confirm modal promises payments are deleted. Keep the block (money history should survive) and fix the modal text + error message, or cascade? I'd keep the block; your call.
- **D-4 (for C-10/C-11) — Events List Edit modal.** Its Status dropdown and Deposit Paid Yes/No bypass the stock pipeline and 062's deposit machinery. Remove both controls from the modal, or route Status through `changeStatus()`?
- **D-5 (for C-08) — fee schedule wiring.** `fm_fee_schedule` is editable in FeesTab and read by **no charging surface** — every charge comes from constants. Wire the four charging surfaces to the table now, or (cheaper pre-walkthrough) soften FeesTab's on-screen claim that a change "takes effect immediately" until the wiring lands?
- **D-6 (for C-40) — deliveries land on the department tier, not the Main Store.** `LogDeliveryTab` calls `applyStockDelta` with no location, so the server falls back to the item's catalogue department. FUNCTIONAL_SPEC §2.1 says the main store is the single inbound point. Point deliveries at `'Main Store'` (one option on one call), or accept department-direct deliveries and amend the spec?
- **D-7 (for C-39) — `fm_approved_items` direct-write policies.** 061's stated design is "the RPC is the only write path so the fee cannot be skipped", and it enforced that on `fm_stall_forfeitures` — but `fm_approved_items` still carries INSERT and DELETE policies for owner/admin (measured). Drop them (a migration, waits behind the 062 rebuild proof), or accept UI-only enforcement?

---

## 2. TOP 10 — fix these first, most important first

1. **C-07** · Movement Ledger shows every requisition fulfil **twice** — 94 live ± pairs typed `'requisition'` that `collapsePairs` doesn't collapse; they dominate the ledger (188 of ~194 routed rows).
2. **C-12** · `shift_settings` still says `Restaurant Bar` / `Front Desk` (never re-tagged) — every Main Bar and Front Office staff row shows Shift "—" in Attendance, and no late/coverage calc runs for them.
3. **C-43** · Admin → Users header is hard-coded "System Users (4)" while the table lists all 8 profiles (the known calibration example — confirmed live at `UsersTab.jsx:92`).
4. **C-10/C-11** · Events List **Edit modal** hand-sets `status` and `deposit_paid` with none of the side effects — confirm via Edit deducts no stock and generates no BEO; cancel via Edit returns nothing; Deposit Paid Yes/No can contradict the 062 payment ledger.
5. **C-37** · **Delete Event errors** on the one event the client will see — `event_payments.event_id` FK is NO ACTION and the event has 5 payments; the modal claims payments will be deleted, then a raw FK message appears.
6. **C-08** · The **fee schedule is decorative**: FeesTab says a change "takes effect immediately", but all four charging surfaces (Market Day fee log, Holders card fees, Payments tab, Messages) charge from constants; only `change_holder_products()` reads the table.
7. **C-18** · Event **confirm breaks for any item holding two department-tier balances** — `maybeSingle()` in the pre-flight throws; HK-005/006/007 are live examples (measured, 2 rows each).
8. **C-02** · There is **no staff clock-in surface at all** — `ClockInOutTab` (the only self-service clock-in, and the only GPS code) is imported by nothing; FUNCTIONAL_SPEC §4 marks Clock In/Out `[DONE]`. If Dhiren asks "how does a staff member clock in?", the answer today is "they can't".
9. **C-29** · Requisition **Fulfil is non-atomic**: stock issues first, the status update is a second call — a failure between them leaves the button clickable and a second click issues stock again.
10. **C-01** · The Dashboard's "deposit unpaid" attention card is **doubly dead**: it gates on `events.deposit_amount` (never written by any code, `0` on the live event) and renders `e.title` (NULL everywhere — the app writes `name`), so when it ever did fire it would show a blank name.

---

## 3. FINDINGS

### Category 1 — Broken or unreachable code

- **C-01 · `src/components/dashboard/OwnerDashboard.jsx:122,176,204` · Dead "deposit unpaid" card, and it renders a column the app never writes.** `unpaidEvents` filters `Number(e.deposit_amount) > 0 && !e.deposit_paid`, but `deposit_amount` is written **nowhere** in `src/` (grep: only these two reads) and is `0` on the live event (measured). The card's `primary` is `e.title`, which is NULL on the live event (measured — the app writes `name`; FOLLOWUPS already records `title` as vestigial). Causes: the Needs-Attention deposit alert can never appear; if the column were ever populated by hand it would render a nameless card. Browser: reverse the live event's deposit, reload the Dashboard → **no** "deposit unpaid" card appears even though the Events List highlights the event amber. **P2** (misleads by omission; the amber list highlight still works).
- **C-02 · `src/components/attendance/ClockInOutTab.jsx` (whole file) · No mount point → the system has no staff clock-in surface.** Nothing imports it (grep: only comments reference it). This is the known FOLLOWUPS item, retained for the QR work — but its consequence for category 9 is new: FUNCTIONAL_SPEC §4 lists "Clock In/Out … [DONE]" and the GPS geofence `[VERIFY]`, and the *only* GPS code in the app lives in this unmounted component. All 15 live attendance rows are manager-written (`user_id` NULL on all — measured). Browser: there is no screen anywhere from which any role can clock themselves in. **P2** — but P1 if the walkthrough script includes staff clock-in.
- **C-03 · `src/components/inventory/LogDeliveryTab.jsx:21,78-82` · The delivery Date field is decorative.** `form.date` is collected, required, and passed nowhere — `applyStockDelta` stamps `now()`. A back-dated delivery is silently recorded as today. Browser: log a delivery dated last week → Movement Ledger shows it dated today. **P3**.
- **C-04 · `src/components/events/EventSetupSection.jsx:110-149` · Unreachable read-only branch.** The `!canManage` render exists, but `/events` is `MANAGE_ROLES`-only, so every role that reaches the page has `canManage` true. Dead branch, no browser symptom. **P3**.
- **C-05 · `src/components/attendance/AttendanceUI.jsx:127-154`, `src/components/attendance/TodayTab.jsx:100`, `supabase/functions/create-user/index.ts:129,147` · `bar_week` residue from the dead bar1/bar2 model.** `getShiftForUser` selects rotating shifts by `user.bar_week`; its only caller is the unmounted C-02. `TodayTab.getShift` hard-codes week `'A'`, so rotating shifts (4 live rows, on the stale `Restaurant Bar`/`Sports Bar` names — see C-12) can only ever resolve to Week A. `create-user` still accepts and stores a `bar_week` body field the UI never sends. Browser: none today (the rotating rows match no staff because of C-12). **P3** — but it becomes live the moment D-2 re-tags the shift rows.
- **C-06 · live DB · Four dead tables, unchanged.** `deliveries`, `inventory_items`, `stock_adjustments`, `stock_transfers`: all RLS-enabled with **0 policies**, 0 rows, and `authenticated` holding unusable INSERT/SELECT/UPDATE grants (measured). No reader or writer in `src/`. Known (FOLLOWUPS drop-decision item) — re-confirmed, not new. **P3**.

### Category 2 — One rule, more than one implementation

- **C-07 · `src/lib/ledger.js:15` vs `supabase/migrations/055_issue_stock.sql:204-214` vs `src/components/inventory/RequisitionsTab.jsx:88-90` · The pair-collapse rule and the pair-writing rule DISAGREE — live, at scale.** `issue_stock` writes a matched ± pair using **the caller's** `p_movement_type` for both legs; requisition fulfil (and `fulfil_requisition_batch`) pass `'requisition'`. `PAIRED_TYPES` is `{'issue','transfer'}` only. Measured: 95 negative + 94 positive `'requisition'` rows (94 pairs + 1 pre-055 single leg); **0** `issue` rows, **0** `transfer` rows — so the collapse code has never had live data, and every routed row a viewer sees except five is half of an uncollapsed pair. It also breaks 057's own doctrine ("one physical event cannot be recorded two ways"): a manual store→bar Transfer records `'issue'`, the identical movement via requisition fulfil records `'requisition'`. Browser: Movement Ledger shows each refill line twice — `−N Main Store → Sports Bar` in red and `+N Main Store → Sports Bar` in green, same timestamp. **P1**. Decision D-1.
- **C-08 · fee amounts exist in FIVE places; the authoritative one is read by nobody who charges.**
  | Copy | Where | Agrees today? |
  |---|---|---|
  | `fm_fee_schedule` (6 rows, measured) | DB — declared authoritative, owner-editable in FeesTab | baseline |
  | `FM_FEES` | `src/lib/constants.js:32-45` | yes |
  | `FM_PAY_TYPES` amounts | `src/components/farmers-market/FarmersMarketUI.jsx:27-34` | yes (4 of 6; no id_card/reprint amounts) |
  | `VISIT_FEE = 10000` | `src/components/farmers-market/MarketDayTab.jsx:8` (also drives the reconciliation strip ×10000 at `:244-246`) | yes |
  | `× 10000` literal | `src/components/farmers-market/HoldersTab.jsx:131` (last-market-day "Collected") | yes |
  All five agree **today**, so nothing is currently wrong on screen — but `fetchFeeSchedule`/`feeAmount` in `src/lib/fm.js` are imported **only by FeesTab** (grep), so the moment the owner edits a fee in FeesTab, every charging surface (Market Day Log Fee prefill and strip, Holders ID-card fees, Payments tab prefills and outstanding column, Messages outstanding lines) keeps charging the old constant — while FeesTab states on screen "Changing an amount here takes effect immediately — no deploy needed" (`FeesTab.jsx:67`). One agreeing-copy trap plus one on-screen claim that is false. Browser: as owner, change the visit fee to 12,000 in Fees, open Market Day → Log Fee still prefills 10,000. **P1** (a self-contradiction demonstrable in two clicks). Decision D-5.
- **C-09 · `src/components/farmers-market/PaymentsTab.jsx:8-22` vs `FarmersMarketUI.jsx:27-34` · Two payment-type lists, and they disagree.** PaymentsTab defines its own `ALL_PAY_TYPES` + `PAY_TYPE_LABELS`, both missing `product_change` (which `fm_payments_payment_type_check_v2` permits and `change_holder_products()` writes). Browser: change a holder's products (raises the 10k fee), open Payments → history Type column shows raw `product_change` instead of a label, and the type can't be hand-logged there (correct, but for the accidental reason of being absent rather than by design like `reversal`). **P2**.
- **C-10 · `src/components/events/EventsListTab.jsx:469-473` vs `EventDetailTab.jsx:199-222` · Two implementations of "change event status" — one has the side effects, one doesn't.** The Edit modal's Status dropdown writes `events.status` directly: enquiry→confirmed via Edit deducts **no** stock, generates **no** BEO (until the detail view next opens and back-fills checklists), and cancelled via Edit **returns no stock** — allocations stay `deducted` forever with the balances short. `changeStatus()` in the detail view does all of it. Browser: allocate stock, confirm via detail (deducts), then Edit → Status → Cancelled → Stock Levels unchanged, allocations still "deducted". **P1**. Decision D-4.
- **C-11 · `src/components/events/EventsListTab.jsx:462-468` · Third writer of `deposit_paid`.** After 062, `deposit_paid` is maintained by Add Payment and recomputed by `reverse_event_payment()`; the Edit modal adds a hand-set Yes/No that can contradict the payment ledger (set "No" while an unreversed deposit row exists, or "Yes" with none). The OwnerDashboard fix (reading `events.deposit_paid` as the single definition) is only as good as this column's writers. Browser: Edit the live event → Deposit Paid → No → the list badge and detail header flip to Unpaid while Payments still shows the deposit unreversed. **P1** (same fix as C-10). Decision D-4.
- **C-12 · Department vocabulary — the canonical 11-value re-tag missed three code-side copies, and one is a live bug.**
  1. **`shift_settings.department` (LIVE BUG):** measured live values include `Restaurant Bar` (2 rotating rows) and `Front Desk` (2 rows); data-ops/003 re-tagged `departments`, `staff`, `stock_items` only. `TodayTab`/`HistoryTab` match shifts by `staff.department`, which is canonical — so **every Main Bar and Front Office staff member matches no shift**: Shift column "—", no late minutes, no overtime flag, and the coverage-alert loop can never fire for those departments. `AddUserTab`/`UsersTab` shift pickers are likewise empty for those departments. Browser: Attendance → Today → Main Bar and Front Office groups show "—" in Shift for every row. **P1**. Decision D-2.
  2. **`DEPT_CODES` in `src/components/admin/StockItemsTab.jsx:6-16`:** keys still `Restaurant Bar`/`Grounds`; `Main Bar`, `Grounds & Landscape`, `Front Office`, `Administration`, `Maintenance`, `Transport` all fall to `'GEN'`. Browser: Admin → Stock Items → add an item with Department "Main Bar" → SKU `GEN-277` (count-based, so also collision-prone across departments sharing `GEN`). **P2**.
  3. **`defaultUnit` same file `:18-23`** keys on `Restaurant Bar` era names — cosmetic default only. **P3**.
  4. **`DEPT_ORDER`/`BEO_TASKS` in `src/components/events/EventsUI.jsx:86-119`:** checklist departments are `Kitchen, Bar, Grounds, Front Desk` — a fourth vocabulary. Freestanding (checklists group on their own values, so nothing breaks), but the walkthrough will show BEO department names that match no department anywhere else in the app. **P3**.
- **C-13 · Market-day maths ×3 — known, re-verified, one new nuance.** `FarmersMarketUI.jsx:60-71` (browser, local clock), `public-checkin/index.ts:66-92` (Deno, fixed UTC+2), `fm_market_day`/`fm_last_n_market_days` (061, `current_date` = **server UTC**). The last-Saturday + no-December rule agrees in all three (read side by side). New nuance: the SQL pair uses the server's UTC date while the Deno copy deliberately uses Malawi time — between 00:00 and 02:00 Malawi time on the day after/of a month boundary or market day, `v_fm_attendance`'s three-day window and the check-in window can disagree about whether a market day "has happened". Two-hour window, monthly rule — cosmetic in practice, but it means the three copies are not byte-equivalent in behaviour, only in rule. **P3** (the ×3 duplication itself is the known STILL-OPEN entry).
- **C-14 · movement_type ×4 — all AGREE (trap, known).** Verified: table CHECK `_v5` (9 values incl. `opening_balance`), `apply_stock_delta` allowlist (8, deliberately minus `opening_balance`), `MOVEMENT_TYPES` in `src/lib/stock.js:34-37` (same 8, absence documented), Movement Ledger `TYPES` (`MovementLedgerTab.jsx:28-38`, all 9). No drift. Recorded so the browser pass doesn't re-derive it. **P3** (trap only).
- **C-15 · "Today's attendance" keyed on two different columns — AGREE today (trap).** `OwnerDashboard.jsx:111,137` filters `date`; `TodayTab.jsx:49` filters `shift_date`. Measured: 0 of 15 rows where `date IS DISTINCT FROM shift_date`, and TodayTab now writes both — but `ClockInOutTab` (if ever remounted) writes only `date` and relies on the DB default for `shift_date` in UTC. The split-brain machinery from AUDIT_2 §3 DoD 6(e) is dormant, not dead. **P3**.
- **C-16 · `src/lib/standards.md:63,76` · The current, follow-me doctrine file names the DEAD role set.** §2 states "Roles are authoritative in src/lib/roles.js: owner, manager, kitchen_manager, restaurant_manager. Four." and its policy template uses `IN ('owner','manager')`. CLAUDE.md instructs sessions to follow this file; a future session writing a policy from its template would gate on a role that no longer exists (fails closed, but silently excludes `admin`). No browser symptom. **P2** (doc trap with a track record — §4.3 of AUDIT_2 is exactly this failure mode).
- **C-17 · `events.name` vs `events.title`** — covered in C-01 (the one live misuse). `title`/`total_amount` have **no writer and no other reader** in `src/` (grep) — both columns are pure schema residue the docs already flag for the Dhiren call. **P3** here; the visible half is C-01.
- **C-18 · `src/components/events/EventDetailTab.jsx:87-91,121-124` and `EventStockSection.jsx:52-61` · Two "how much is available" implementations, each wrong for multi-row items.** Both assume one department-tier `current_stock` row per item. The detail-tab pre-flight and confirm loop do `.eq('tier','department').maybeSingle()` — which **throws** ("multiple rows returned") when an item holds two department-tier balances. The stock section builds `qtyMap` keyed by item, so a second row silently overwrites the first (arbitrary order — the Laundry balance can mask the Housekeeping one). Measured live: HK-005, HK-006, HK-007 each hold 2 department-tier rows (Housekeeping + Housekeeping/Laundry); any store→department issue to a second department creates more such items on the fly. Neither path names a location, and the actual deduction falls back to the item's catalogue department — so even the "one row" case checks a row it may not deduct from once items live in more than one department. Browser: allocate HK-006 to an event, click Confirm Event → error toast with a raw PostgREST "JSON object requested…" message, event stays enquiry. **P2** (P1 if the walkthrough allocates a Housekeeping item).
- **C-19 · Five near-identical helper sets — known since AUDIT_1, still true.** `fmtDate`/`fmtMWK`/`todayStr`/`AccessDenied`/`EmptyRow` are defined in `EventsUI.jsx`, `FarmersMarketUI.jsx`, `AttendanceUI.jsx`, `AdminUI.jsx`, `InventoryUI.jsx`, `TableBookingsUI.jsx` (six now, counting bookings). They agree in behaviour except `AdminUI.fmtDate` parses date-only strings as UTC midnight (renders the same day at UTC+2 — currently harmless). Trap, not a bug. **P3** (see C-49).
- **C-20 · `PaymentsTab.holderOutstanding` (`:101-107`) vs `MonthlyMessagesTab.buildIndividualMessage` (`:108-111`) · Outstanding-fees rule duplicated.** Same three components (application, acceptance, this-month visit) computed independently; they agree today, including both reading `FM_FEES`. Trap — and the WhatsApp message quotes amounts to the *client-facing* stallholder from the constants, so C-08's fee edit would make the messages wrong too. **P3**.
- **C-21 · Market hours 07:30 / 12:30 in three places.** `CheckIn.jsx:6-9` (constants + the two banner texts), `MonthlyMessagesTab.jsx:90,116` (message text). Agree today. **P3**.

### Category 3 — Silently-empty reads

The screens with a real "empty because broken vs empty because correct" ambiguity, for the browser pass:

- **C-22 · `src/components/farmers-market/HoldersTab.jsx:111-112` · Attendance and taxonomy errors are swallowed by design.** `fetchAttendance().catch(() => ({byHolder:{}, marketDates:[]}))` and `fetchTaxonomy().catch(() => null)`. If `v_fm_attendance` or the three taxonomy reads fail for ANY reason (RLS regression, view dropped on a bad rebuild, network), the tab renders exactly like no-data: every holder shows "No attendance window available", every product list shows "Not yet classified", the forfeit-eligible banner silently disappears, and **no toast fires**. Browser: you cannot distinguish this from real data absence by eye — if every single holder shows "No attendance window available", suspect a failed read, not empty data (with 687 visits live, at least the Z-holders must show grids). **P2**.
- **C-23 · Systemic: reads that discard `error` and render the empty state.** These fetches destructure `{ data }` only, so a failed read is indistinguishable from an empty table: `InventoryUI.fetchActiveItems/fetchDepartmentList/fetchUserMap/fetchStaffUsers` (`InventoryUI.jsx:60-92` — feeds item/department/staff dropdowns on five tabs: a failure silently empties the pickers); `EventsListTab.load` (`:127-141` — the **Events List itself** renders "No records found" on a failed read, and the revenue strip computes from `[]`); `EventStockSection.loadAllocations/loadStockItems`; `EventStaffSection` both loads; `BarCountTab.fetchRecent`; `MarketDayTab.load`; `PaymentsTab.load`; `MonthlyMessagesTab` both loads; `UsersTab.fetchUsers/fetchDepartments`; `AddUserTab.fetchDepartments/fetchShifts`; `StaffTab.fetchStaff`; `StockItemsTab` both; `OwnerDashboard.load` (all six queries). Counter-examples that DO surface errors (use these as the reference pattern): `StockLevelsTab`, `MovementLedgerTab`, `ConsumptionTab`, `RequisitionsTab.fetchReqs`, `BarCountTab.fetchLocations/fetchRows`, `FeesTab`, `WaitingListTab`, `RoomsTab`. **P2** as a class — during the walkthrough a transient failure on any of the silent screens looks like missing data.
- **C-24 · `src/contexts/AuthContext.jsx:6-13` · A failed profile fetch silently becomes "no role".** `fetchProfile` ignores `error`; if the `user_profiles` read fails, `profile` is null and `GuardedPage` bounces a validly-signed-in user to `/login` with no message. Browser symptom: sporadic "logged in but thrown back to login" with no error — if seen, this is the cause, not auth. **P2**.
- **C-25 · Role-scoped blanks that read as missing data (correct behaviour, listed so the browser pass doesn't file them):** a `department_head`'s Movement Ledger omits all 533 `opening_balance` rows (no route → not theirs) and shows only movements touching their department; their Requisitions "Requested By" and Ledger "Performed By" columns are blank for other users (`fetchUserMap` returns own row only under `user_profiles_own_select` — known FOLLOWUPS item); Kitchen/Restaurant/etc. heads see 0 stock rows (data absence — their departments hold no stock except Housekeeping). All verified consistent with the measured policy set. **P3** (expected; brief the walkthrough).
- **C-26 · `src/components/inventory/StockLevelsTab.jsx:57-64` and `ConsumptionTab` location lists · Filters offer locations that must be empty.** Both build from the full `departments` table (+ Main Store); 8 of 11 departments hold no stock. Choosing one shows "No items at this location" / "holds no stock yet" — labelled, so distinguishable, but the dropdowns imply stock exists in departments that have none. Known class (FOLLOWUPS, 27 July). **P3**.
- **C-27 · `src/components/dashboard/OwnerDashboard.jsx:133-137,166` · Unverified-attendance cards name people via a join that is NULL for every manager-written row.** The query joins `user_profiles!user_id`, but all 15 live rows have `user_id` NULL (measured) — a manager Override to "Unverified" produces a card titled **"Unknown staff"**. Browser: Attendance → Today → Override someone to Unverified → Dashboard shows "Unknown staff clocked in off-site, needs review". **P2**.
- **C-28 · `src/components/inventory/BarCountTab.jsx:43-57` · An owner with no par-managed locations sees an empty "Select bar…" dropdown** indistinguishable from a broken read (fetchLocations does flash errors, so only the RLS/empty-data case is ambiguous). Today Main Bar (276) + Sports Bar (283) have par rows (measured), so the dropdown is populated. **P3** (note for post-dedupe: the dedupe rewrite must keep par rows or this tab goes blank).

### Category 4 — Non-atomic write paths

Multi-write browser sequences, what order, and the stranded state if a later step fails. `apply_stock_delta`/`issue_stock`/`transfer_stock`/`post_bar_count`/`fulfil_requisition_batch`/`reverse_event_payment`/`forfeit_stall`/`change_holder_products` are all atomic server-side — the gaps below are all in the browser between calls.

- **C-29 · `src/components/inventory/RequisitionsTab.jsx:73-99` · Fulfil: stock moves, then status updates.** `issueStock(...)` (atomic, both balances + both ledger rows) then a separate `requisitions.update({status:'fulfilled'})`. If the second call fails, stock has moved but the requisition still reads `approved` with a live **Fulfil** button — a second click issues the full quantity again. No RPC exists for single-requisition fulfil (`fulfil_requisition_batch` covers only count batches and marks status inside the transaction — the pattern the loose path should share). Browser: not provokable on demand, but a double-issued balance with two identical ledger pairs is the signature. **P2**.
- **C-30 · `src/components/events/EventPaymentsSection.jsx:87-110` · Add Payment: payment insert, then `deposit_paid` update.** A deposit that inserts but whose `events` update fails leaves the amber "unpaid deposit" highlight lying in the other direction from 062's reversal machinery (which is atomic precisely because of this class). **P3**.
- **C-31 · `src/components/events/EventStockSection.jsx:86-128` · Add-allocation-when-confirmed: 3 steps with a partial compensator.** Insert allocation → deduct (on failure: compensating delete — good) → update status to `deducted`. If step 3 fails, stock is deducted but the allocation stays `pending`; the next Confirm/`handleStockOnStatusChange` pass deducts it **again**. **P2**.
- **C-32 · `src/components/events/EventStockSection.jsx:168-195` · Clearance loop: per-row return then update.** If a row's `update` fails after its `applyStockDelta` return succeeded, the row stays `deducted`; re-running clearance returns that stock a **second** time. Also the loop is per-row, so a mid-loop failure leaves a half-cleared event (survivable — re-run clears the rest). **P2**.
- **C-33 · `src/components/events/EventDetailTab.jsx:199-222` · Event confirm: status first, then the per-allocation loop.** Known and explicitly logged ("multi-item event confirm is still not transactional — do not assume otherwise"). Re-verified unchanged: `events.status` is written **before** `handleStockOnStatusChange`, and each allocation is a separate deduct+update pair. The pre-flight makes the common failure (insufficient stock) safe; an uncommon mid-loop failure leaves a confirmed event partially deducted. STILL-OPEN, listed here only for completeness of this category.
- **C-34 · `src/components/farmers-market/HoldersTab.jsx:368-426` · ID card issue: card insert, then payment insert.** Failure between them = card on record, fee never charged (silently — the modal already closed). Reprint path likewise (status update then payment). **P3**.
- **C-35 · `src/components/farmers-market/MarketDayTab.jsx:135-179` · Log Fee and Remove both `Promise.all` two writes.** Log Fee: `fm_payments` insert ∥ `fm_visits.fee_paid=true` — if the insert fails and the update succeeds, the strip counts "Collected" money (`fee_paid × 10000`) that has no payment row. Remove: visit delete ∥ payment delete — partial failure leaves a paid-but-unvisited or visited-but-refunded remainder. **P3**.
- **C-36 · `src/components/farmers-market/PaymentsTab.jsx:133-167` · Log Payment: insert, then per-type flag updates** (`application_paid` / `acceptance_paid` / `fm_visits.fee_paid`), unchecked for errors (no `throw` on the follow-ups). A failed flag update flashes success anyway. **P3**.
- **C-37 · `src/components/events/EventsListTab.jsx:234-244,499-518` · Delete Event: single call, but the DB refuses it for the event the client will see — and the modal lies.** Measured FK rules: `event_bill_items`/`event_checklists`/`event_configurations`/`event_staff`/`event_stock_allocations` all CASCADE; **`event_payments.event_id` is plain `REFERENCES events(id)` (NO ACTION)**. The live event has 5 payments, so Delete throws an FK violation rendered as a raw toast, while the modal text promises "permanently deletes the event along with all checklists, **payments**, and bill items". Filed under this category because the failure mode is a stranded confirm dialog + raw error, not data loss. Browser: Events → Delete on the wedding → error toast quoting the constraint name. **P1**. Decision D-3.

### Category 5 — UI gate vs RLS disagreement

Compared every screen's gates against the full measured policy dump (149 policies) and grant matrix. Route guards vs `ROUTE_ACCESS`: **all seven routes agree** with `App.jsx` and Sidebar; no route mounts a component whose own gate names a role the guard blocks (the old `AT_MANAGE_ROLES` dead-gate class is gone — see Already Closed). Direction-classified results:

**UI WIDER than the database (the dangerous direction):**
- **C-37** (above) — Delete offered, FK refuses. The only live case found where a rendered control is guaranteed to error under normal use.
- **C-38 · `src/components/attendance/SettingsTab.jsx:96-104` · Shift delete is offered to hr and permitted — verify intent.** `shift_settings_manage_delete_v2` exists for owner/admin/**hr** (measured), so the button works; flagged only because every other reference table (departments: owner-only; rooms/stock_items: no delete at all) is stricter, and shift deletion under a `window.confirm` is the loosest destructive control in the app. Not an error path — a consistency question. **P3**.
- **C-39 · `fm_approved_items` — RLS wider than the 061 design.** Measured: `fm_approved_items_manage_insert_v2` and `_manage_delete_v2` exist for owner/admin alongside the RPC. 061's own rationale ("a direct insert would change the products and skip the charge") is enforced only by the UI having deleted its direct-write path; PostgREST still accepts one from an owner/admin session. Not reachable from any screen. **P3**. Decision D-7.
- **C-40 · `event_payments_manage_update_v2` survives 062.** Owner/admin can still UPDATE payment rows via PostgREST although the whole 062 design is corrections-by-reversal ("the original figure survives its own correction"). No UI edits payments, so this is unreachable from a screen — but the append-only property the docs describe is UI-deep, not policy-deep (only DELETE is truly closed). **P3**.
- **C-41 · `src/components/admin/DepartmentsTab.jsx:33-53` · Rename/Delete department ripples nowhere, and the guard trigger makes the aftermath worse, not better.** Departments are plain text by standing rule; six tables store the name (`staff`, `stock_items`, `current_stock.location`, `user_profiles.department`, `shift_settings`, `stock_movements.from/to`). Renaming leaves every one of them on the old name: the renamed department's head sees nothing, its stock rows become location-orphans, and 054's location guard then **rejects any new movement** to the old name (the exact orphan class data-ops/004 cleaned up). RLS permits it (owner). Browser: Admin → Departments → rename "Kitchen" to "Kitchens" → Kitchen head's Stock Levels goes empty, transfers to "Kitchen" start failing. One click, system-wide, no warning. **P2** (P1 if Dhiren is likely to touch that screen live).

**UI NARROWER than RLS (fails closed — cosmetic, listed for the record):**
- **C-42 · Confirmed narrower-than-policy gates, all safe:** TodayTab hides **Mark All Absent** from hr (`MANAGE_ROLES` at `TodayTab.jsx:265`) though `attendance_manage_insert_v2` includes hr — hr can Override each person but not bulk-mark. RequisitionsTab shows a head only their own requisitions where RLS grants the whole department (known STILL-OPEN, `RequisitionsTab.jsx:29`). hr holds staff INSERT/UPDATE policies with no reachable screen (known — StaffTab sits under owner-only `/admin`). PaymentsTab (FM) renders no role gate of its own but is unreachable below owner/admin via the route. Everything else lines up: events/bookings/FM writes owner-admin both sides; bar-count head policies exactly match the tab's behaviour (draft-only, own-location, `counted_by = auth.uid()`); stock writes owner/admin both sides; `stock_items` writes owner-only matching the owner-only Admin screen; rooms/fees/waiting-list match their tabs. **No action.**

### Category 6 — Hardcoded values that should be data

(Fee amounts are C-08; department vocabularies are C-12. Remaining:)

- **C-43 · `src/components/admin/UsersTab.jsx:92` · `System Users (4)` — a hard-coded count in the header.** Live profiles: **8** (measured; all active). The table below it lists all 8. This is the calibration example from the brief, confirmed at its exact location — the count is a string literal, not a computation. Browser: Admin → Users says (4), lists 8. **P1** (a screen contradicting itself in the first module an owner checks).
- **C-44 · `src/components/attendance/AttendanceUI.jsx:4-6` · Lodge coordinates and 100 m radius are code constants.** `LODGE_LAT/LODGE_LNG/RADIUS_M` have no DB source. Only consumed by unmounted C-02 today, so inert — but the QR attendance build will need them server-side (a browser constant is trivially spoofable, which is half the reason the geofence exists). **P3**.
- **C-45 · `src/components/admin/AddUserTab.jsx:63` · Supabase project URL hard-coded** in the `create-user` fetch (`https://gttsjmxltrxxfplqjans...`) instead of `import.meta.env.VITE_SUPABASE_URL`. Open since AUDIT_1 §2.7. **P3**.
- **C-46 · Status/threshold literals that are fine but enumerable** (recorded so the list is complete, no action implied): Mark-All-Absent gate `ELEVEN = 11` (`TodayTab.jsx:13` — spec rule); no-show 45 min (`TableBookingsUI.jsx:44` — spec rule); market hours 07:30/12:30 (C-21); consecutive-absence threshold 2 over a 3-day lookback (`TodayTab.jsx:79-85` — no spec source, invented default); overtime flag = shift end + 30 min (`TodayTab.jsx:123` — invented default); `ROW_LIMIT` 500/1000 (Consumption/Ledger — both render a truncation notice, correct). **P3.**

### Category 7 — Handover blockers (one list, one job)

**C-47 · Everything that must not ship to the client, with locations:**

| # | What | Where | Note |
|---|---|---|---|
| 1 | `detail` field on public error responses | `supabase/functions/public-checkin/index.ts:142` | Known, logged twice; still present |
| 2 | Raw error text rendered on the public check-in page | `src/pages/CheckIn.jsx:155-157` (+ extraction at `:24-29`) | The client-side half of #1 — strips with it |
| 3 | New-user password echoed in plaintext on screen | `src/components/admin/AddUserTab.jsx:100` | Open since AUDIT_1; also the password input is `type=text` (`:127`) |
| 4 | Internal project narrative in client-facing UI | `EventsUI.jsx:258-266` (`ProvisionalRevenueNote`: "…raised on 27 July and never made specific… **Dhiren** picks one on his return"), `FeesTab.jsx:65` ("All six confirmed by Dhiren, 18 August 2026"), `HoldersTab.jsx:818` ("From the Feb 2026 register: …") | Deliberate for the walkthrough; must be rewritten/removed at handover. The revenue note is *designed* non-dismissible — it will be read aloud |
| 5 | Decorative search pill + notification bell with a hard-coded red "unread" dot | `OwnerDashboard.jsx:249-263` | Known FOLLOWUPS item ("wire up or remove") — the fake unread dot is the misleading part |
| 6 | Placeholder FM demo data live in production | 6 `Z00n` holders, 687 visits (mostly seeded), 8 waiting-list rows, 72 approved items (data-ops/008) | Known purge item — one delete keyed on the `Z` prefix + placeholder notes |
| 7 | Placeholder rooms (24), Housekeeping catalogue (7 `HK-` items), flat bar quantities, par = 2× reorder, uniform reorder levels | migrations 053/059/060 seeds | Known, by design until real data |
| 8 | Demo residue in Requisitions/Ledger: 190 par-refill requisitions (94 fulfilled / 96 rejected) and their 188 ledger rows from the 17 Aug test counts | live data | Decide with D-1 whether these rows are re-typed, kept, or purged as test data before handover |
| 9 | Hard-coded "(4)" header | C-43 | Fix pre-walkthrough anyway |
| 10 | Real staff PII in a git-tracked migration | `supabase/migrations/016_staff_restructure.sql` | Known since AUDIT_2 §2.7; a handover-repo decision |
| 11 | `deliveries` + 3 dead tables | C-06 | Drop-or-wire decision, logged in FOLLOWUPS |
| 12 | Deactivated users would still appear in the Events "Received By" picker | `EventPaymentsSection.jsx:40` (no `is_active` filter) | Latent until someone is deactivated |

No `console.log` of any kind and no TODO/FIXME/HACK comments exist in `src/` (grepped) — the two classic categories are clean.

### Category 8 — Simplification (delete-only)

- **C-48 · Deletions that remove a second copy or dead weight, nothing else:**
  1. `feeAmount()` in `src/lib/fm.js:114-118` — exported, imported by nothing (grep). Delete — or it is the seam D-5 wires the charging surfaces through; either way its current state (dead) is the worst of both.
  2. `ALL_PAY_TYPES` + `PAY_TYPE_LABELS` in `PaymentsTab.jsx:8-22` — delete, import `FM_PAY_TYPES` from `FarmersMarketUI.jsx` (which already has the amounts and `product_change`). Fixes C-09 by deletion.
  3. `VISIT_FEE` in `MarketDayTab.jsx:8` and the `* 10000` in `HoldersTab.jsx:131` — delete in favour of `FM_FEES.visit`. Two of C-08's five copies gone.
  4. The `!canManage` branch of `EventSetupSection.jsx:110-149` — unreachable (C-04).
  5. `bar_week` passthrough in `create-user/index.ts:129,147` — the UI never sends it; deleting it removes the last writer of a dead column.
  6. The `form.date` field in `LogDeliveryTab.jsx` — currently decorative (C-03); delete it or wire it, but not the current state.
  7. `getShiftForUser` in `AttendanceUI.jsx:127-154` — its only caller is unmounted C-02. If C-02 is retained for QR, keep; otherwise both go together. (Flagged, not urged — FOLLOWUPS records the retention decision.)
  8. The six-way `fmtDate`/`fmtMWK`/`AccessDenied`/`EmptyRow`/`todayStr` duplication (C-19) — five copies deletable into one `lib/format.jsx`. Post-walkthrough; churny.
  Nothing here touches a proven-live RPC or an RLS policy.

### Category 9 — Spec-to-code reconciliation

Every FUNCTIONAL_SPEC section, checked claim-by-claim against code and the measured DB. Verdicts: DONE-AND-WIRED / DONE-AT-DB-ONLY / PARTIAL / NOT FOUND.

| Spec item | Verdict | Evidence / what's missing |
|---|---|---|
| §1 role model + route table | **DONE-AND-WIRED** | `roles.js` matches the spec table exactly; guards/Sidebar agree; 5 head accounts + hr live (measured) |
| §2.1 two-tier inventory | **DONE-AND-WIRED** with one spec breach | **Deliveries credit the item's department tier, not the Main Store** — `LogDeliveryTab` passes no location, `apply_stock_delta` falls back to `stock_items.department` (055:104-106). "One inflow" (§2.1) is not what the Log Delivery tab does. **P2**, decision D-6. Browser: log a delivery for SBA-1001 → Sports Bar balance rises, Main Store unchanged |
| §2.2 rooms | **DONE-AND-WIRED** | 24 rows live; RoomsTab has add/inline-edit/deactivate/show-inactive/count; no delete control and no DELETE policy (measured) |
| §2.3 consumption ledger | **DONE-AND-WIRED** | View + tab + RPC all wired; 2 live draw rows (measured). Retention floor unenforced — known/logged. Bar leg currently contributes 0 rows (both 17 Aug counts had counted = system on every line — measured) |
| §3 Stock Levels / Log Delivery / Requisitions / Transfers / Adjustments | **DONE-AND-WIRED** | Columns/filters as specified; Par column present. Log Delivery: see §2.1 row. Requisitions head-view narrower than RLS (known) |
| §3 Movement Ledger | **PARTIAL** | All types, filters, preset, Supplier column present — but the pair-collapse contract is broken for the dominant live type (C-07), and `sub_location` (060) is neither selected nor shown, so a Laundry movement reads as plain Housekeeping (**P3**) |
| §3 Bar Count (059) | **DONE-AND-WIRED** | Pre-filled sheet, shortfall preview, post → refill; 2 posted sessions + 190 refill requisitions live prove the whole cycle has executed against production |
| §4 Attendance Today/History/Settings | **DONE-AND-WIRED** | All three tabs; Mark-All-Absent 11:00 gate; department filter — but see C-12 (shift lookup broken for 2 departments) |
| §4 Clock In/Out `[DONE]` | **NOT WIRED** | Component exists, mounted nowhere (C-02). The `[DONE]` marker is wrong as a UI claim |
| §4 GPS geofence `[VERIFY]` | **ANSWERED** | Logic exists and is sound: 100 m haversine vs `LODGE_LAT/LNG`, 5 s timeout, outside/unavailable → `unverified` + `within_radius=false`, inside → late-vs-present (`ClockInOutTab.jsx:81-142`). But it is only reachable through the unmounted C-02, so **GPS clock-in is currently dead code**. The manager views do render the radius flag (`TodayTab.jsx:382-388`, `HistoryTab.jsx:301-307`) |
| §4 QR staff attendance `[NEW]` | **NOT BUILT** | As expected — the one Phase 2 feature not started; `/attend` route in the spec's table does not exist in `App.jsx` |
| §5 Events list/create/detail/BEO/bill | **DONE-AND-WIRED** | With C-10/C-11 (Edit modal bypass) and C-37 (delete) as the defects |
| §5 Payments editable (062) | **DONE-AND-WIRED** | Reverse button owner/admin, reason required, strikethrough + badge, reversal row names its original, `reversal` absent from Add dropdown, Recorded By column present — all verified in code; RPC + trigger + partial unique index live (measured in `pg_proc`/constraints) |
| §5 Revenue display | **DONE-AND-WIRED** | Three readings, shared picker, localStorage persistence, non-dismissible provisional note on both surfaces, net-of-cost refuses to fake a margin. (Note the provisional text is handover item C-47#4) |
| §6 Table Bookings | **DONE-AND-WIRED** | No Phase 2 changes; gates match measured policies (insert/update owner-admin, cancel-by-update, no delete anywhere) |
| §7 Market Day / Holders / Add / Payments / QR check-in | **DONE-AND-WIRED** | Stall regex fix confirmed in both Add and Edit (`STALL_RE` 3-digit, shared) |
| §7 attendance history + forfeiture (061) | **DONE-AND-WIRED** | Attendance grid per holder, forfeit-eligible banner + confirm modal → `forfeit_stall()`; eligibility server-side. Only the 6 `Z` placeholders can be eligible (real cohort `created_at` = import date — known, decision owed) |
| §7 3-level taxonomy + product picker | **DONE-AND-WIRED** | Picker renders category › type › item; writes only via `change_holder_products()`; direct-write UI deleted (C-39 notes the policies survived). **But** the Holders summary strip, filter tabs, Add and Edit forms still run on deprecated `stall_type` (all 311 = 'Other', measured) — the taxonomy exists beside, not instead of, the old classification on those surfaces. **PARTIAL** on the display half, **P3** (known deferral, but the walkthrough will show "Produce: 0 · Crafts: 0 · … Other: N") |
| §7 fee schedule | **PARTIAL** | Table + FeesTab wired; **no charging surface reads it** (C-08) |
| §8 Admin Users/Add/Departments/Stock Items | **DONE-AND-WIRED** | With C-43 (count), C-12.2 (SKU codes), C-41 (department rename ripple) |
| §8 Rooms | **DONE-AND-WIRED** | See §2.2 |
| §9 cross-module rules | All hold in code as stated except: "One inflow, many outflows" (broken by Log Delivery — D-6) and "GPS outside radius → unverified" (dead code — C-02) |
| §10 Add-User bar1/bar2 `[VERIFY]` | **ANSWERED** | Gone from `AddUserTab` (dropdown derives from `ROLE_LABELS`; rotating shifts excluded from selection). Residue: `create-user` accepts `bar_week` (C-05/C-48.5), and `AttendanceUI` still contains bar-week shift logic used only by dead code |

---

## 4. SIX-SCREEN VERDICT

| Screen | Status | Most likely to fail when you click it |
|---|---|---|
| Inventory → Bar Count | DONE-AND-WIRED (already exercised live: 2 posted sessions) | Posting a second count for a bar on a date already posted → clean refusal message (correct). Pick a fresh date. As a bar head, the Bar field pins correctly |
| Inventory → Consumption | DONE-AND-WIRED (2 live draws) | Most locations show "holds no stock yet" (only Housekeeping + bars hold department stock); the ledger shows only the 2 draws — bar leg is legitimately empty because both counts were flat. If EVERY holder/filter combination looks dead, suspect C-23-class silent failure |
| Admin → Rooms | DONE-AND-WIRED (24 rooms) | Nothing found. No delete control, deliberately — don't read its absence as a bug |
| FM taxonomy / forfeiture / product picker | DONE-AND-WIRED | Forfeit banner lists only Z-holders (correct on this data). Summary strip still counts by `stall_type` → "Other: 305". If attendance grids say "No attendance window available" on every holder → C-22 swallowed error, not empty data |
| Events → Revenue panel | DONE-AND-WIRED | Net of Cost shows "—" + the no-cost-source explainer (deliberate). The provisional note names Dhiren on screen (C-47#4) |
| Events → Payments Reverse | DONE-AND-WIRED | Reverse the deposit → header + list flip to Deposit Unpaid (correct); the Dashboard will NOT show an unpaid-deposit card (C-01). Don't reverse then hand-edit deposit_paid in the Edit modal (C-11) or the two will disagree |

---

## 5. BROWSER-CHECKABLE PREDICTIONS

Exact clicks to confirm or refute each finding with a visible symptom:

| ID | Where to click | What you should see if the finding is real |
|---|---|---|
| C-07 | Inventory → Movement Ledger, no filter | Every refill line twice: `−N` red and `+N` green, both "Main Store → <bar>", same timestamp, type Requisition — ~188 such rows dominating the list |
| C-12 | Attendance → Today | Main Bar and Front Office staff groups: Shift column "—" on every row; other departments show times. Also Admin → Users → Edit a Main Bar user → no shift picker |
| C-43 | Admin → Users | Header "System Users (4)", table lists 8 rows |
| C-10 | Events → Edit (pencil) on the wedding → Status → Cancelled → Save | Status flips; Stock Allocations still say "deducted"; Stock Levels unchanged (nothing returned) |
| C-11 | Events → Edit → Deposit Paid → No → Save | Badge flips to Unpaid while Payments shows the deposit un-reversed |
| C-37 | Events → Delete on the wedding → Delete | Red toast quoting a foreign-key violation; event survives; modal promised payments would be deleted |
| C-08 | FM → Fees (as owner) → edit Visit fee to 12,000 → Market Day → Log Fee | Modal still prefills 10,000; reconciliation strip still multiplies by 10,000 |
| C-18 | Events → Stock Allocations → add HK-006 (Laundry Detergent) → Confirm Event | Error toast with raw "JSON object requested, multiple (or no) rows returned"; event stays enquiry |
| C-01 | After reversing the only deposit: Dashboard | Events List highlights amber, but Needs Attention shows **no** deposit card |
| C-27 | Attendance → Today → Override anyone to Unverified → Dashboard | Needs Attention card titled "Unknown staff" |
| C-02 | Any role, whole app | No screen anywhere offers "Clock In" |
| C-03 | Inventory → Log Delivery with last week's date | Ledger shows the delivery dated today |
| C-09 | FM → Businesses → Change Products (make a real change) → Payments → history | Type column shows raw `product_change` |
| C-12.2 | Admin → Stock Items → add item, Department "Main Bar" | SKU generated as `GEN-…` |
| C-41 | **Don't do this on production** — Admin → Departments → rename any stocked department | That department's head loses all stock visibility; transfers to the old name error |
| C-22 | FM → Businesses | If *every* holder shows "No attendance window available" → failed read, not data; Z-holders must show grids on real data |
| C-26 | Inventory → Stock Levels → filter "Security" | "No items at this location" — correct-empty, don't file it |
| C-25 | As a bar head → Movement Ledger / Requisitions | Only own-department movements; blank Performed By/Requested By names — correct under RLS, don't file it |
| C-47#5 | Dashboard top bar | Bell shows a red dot; clicking does nothing; search pill inert |

## 6. STILL-OPEN FROM FOLLOWUPS — confirmed still live

`public-checkin` `detail` field (index.ts:142 + CheckIn.jsx:156) · QR raw-UUID / no rate limit · market-day maths ×3 (all copies agree today — C-13) · multi-item event confirm non-transactional (C-33) · RequisitionsTab head sees own-only where RLS grants department · `fetchUserMap` degradation for heads · hr staff-write with no reachable UI · hr bounced by the `*`→`/` catch-all (App.jsx:45 unchanged; `getDefaultRoute` exists but the catch-all doesn't use it) · ClockInOutTab unmounted (retained for QR) · notification bell + search decorative · AddUserTab plaintext password + hardcoded URL · no par-level editing UI · no delete-user path · ID card #3 price unconfirmed (charged at replacement rate, documented in constants.js) · imported cohort `created_at` = import date → no real holder forfeit-eligible · Z-placeholder purge owed · `stall_type` uniformly 'Other', deprecated in place (311/311 measured; 50 holders carry `category_id`, all placeholder-assigned) · catalogue dedupe + table split deferred · `deliveries` + 3 dead tables drop decision (C-06) · `handle_new_user` orphan function (present in `pg_proc`, no trigger) · `attendance_records` ordinal divergence (accepted) · **062 rebuild proof owed before any 063** · 12-month consumption retention unenforced · `event_bill_items` `amount > 0` forbids comped lines (Dhiren question) · legacy `service_role` policy residue — with a correction: **`user_profiles` carries a true duplicate pair** ("service role full access on user_profiles" + "service_role_all_user_profiles", both measured); **`departments` carries only the legacy-named single policy** ("service role full access on departments") — there is no duplicate pair there, just a non-canonical name. The FOLLOWUPS phrasing "2 legacy duplicate policies (departments, user_profiles)" is half right; re-check before the cleanup drop.

## 7. ALREADY CLOSED — docs claim open, code says fixed

- **The `AT_MANAGE_ROLES`-lists-`restaurant_manager` dead gate is gone.** The constant now lives in `roles.js:27` as `['owner','admin','hr']`; no component gates on a role the router blocks (whole-src grep). The audit brief and FOLLOWUPS (Session B note) still cite it as live.
- **A Housekeeping `department_head` account now exists** (measured: `department_head | Housekeeping`, active). FOLLOWUPS 4e ("no Housekeeping head account exists — needs creating") is stale.
- **The Bar Count / refill cycle HAS been exercised against production** — 2 posted sessions (Main Bar + Sports Bar, 17 Aug) and 190 par-refill requisitions (94 fulfilled, 96 rejected) are live, and 2 consumption draws exist. STATE's open items "Browser-verify the Bar Count tab / Consumption tab" appear overtaken by events (someone posted and fulfilled for real); STATE also still says "0 consumption movements" post-rollback, which no longer matches (727 movements, 2 consumption rows). Reconcile STATE to the measured state.
- **Stall-number regex** — confirmed closed in code exactly as documented: one `STALL_RE = /^[A-Za-z]+\d{3}$/`, used by both Add and Edit.
- **Add User bar1/bar2 branching** ([VERIFY] item 4) — gone from the UI; only the inert `create-user` passthrough and dead `AttendanceUI` logic remain (C-05).
- **`is_active` enforcement** (AUDIT_2 §2.5(b)) — closed three ways: `GuardedPage` check, `Login` sign-out-at-the-door, and `current_app_role()` returning NULL for deactivated profiles.
- **`fm_market_days` exists and works** (migration 026 + `MarketDayTab` autosave with surfaced errors) — AUDIT-era finding long closed, confirmed.

## 8. NOT AUDITED

- **The built bundle and deployed artefacts** — no `npm run build`, no `dist/` inspection, no check that the *deployed* Edge Functions match the repo source (the repo copies were audited; deployment parity is unverifiable read-only from here).
- **Dependency CVEs** — `npm audit` not run (AUDIT_2's attempt failed on the registry; unresolved).
- **Behaviour as each role in a browser** — this audit ran no UI. All role claims are derived from the measured policy/grant dump plus code reading; your parallel browser pass is the runtime half.
- **Migrations 001–058 line-by-line** — covered by the §2.6 rebuild proofs (runs 4–9); only 055 and 061 were re-read in full here, plus targeted greps. 062's SQL was not re-read (11/11 live proof accepted); its rebuild proof remains owed.
- **`scripts/data-ops/` contents** — treated as historical record, not re-verified.
- **Table Bookings deep pass** — gates and write paths verified (all four tabs); per-field UX not re-audited (no Phase 2 changes, module previously exercised).
- **Realtime subscription behaviour** (`MarketDayTab` channels) — code read, not exercised; Supabase Realtime requires publication config that cannot be confirmed read-only from the schema alone. If the "Live" indicator never turns green on market day, check the `fm_visits`/`fm_payments` publication before blaming the tab.
- **Vercel/env parity** (`.env.local` vs deployed) — out of read-only scope.

---

*End of audit. One file changed: this one.*
